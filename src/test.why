func makeAdder (offset) {

  func add (x, y)  {
    x + y + offset
  }

  add
}

const adder = makeAdder(1);
let foo = 50 / 2;

const obj = {
  x: 100,
  y: 32,
  foo: foo,
  complex: {
    bar: true,
  },
};

let f = obj.complex.bar;
foo = obj.foo + 5

print(f, foo)
print([1,3,5])
print(adder(10.5, 5))